1.1 Integer Exercise: Calculate the area of a rectangle with a length of 15 and a width of 8 using integer arithmetic.
1.2 Float Exercise: Calculate the average of three test scores: 85.5, 92.0, and 78.5.
1.3 String Exercise: Create a string variable containing the phrase "Python is fun!". Concatenate it with another string variable containing " Let's learn it!". Print the combined string.
1.4 Boolean Exercise: Write a program that checks if a number is both greater than 10 and less than 20. Print True if it is, and False otherwise. Test with the numbers 5, 15, and 25.

2.1 Write a Python program to calculate the area of a triangle given its base and height.
2.2 Write a Python program to convert Celsius to Fahrenheit using the formula: F = (C * 9/5) + 32.
2.3 Write a Python program to calculate the simple interest given the principal amount, rate of interest, and time period

3.1 Write a Python program that takes two numbers as input and prints whether the first number is greater than, less than, or equal to the second number.
3.2 Write a Python program that takes a string as input and checks if it is equal to a specific word (e.g., "Python").
3.3 Write a Python program that takes an age as input and checks if the person is eligible to vote (age >= 18).

4.1 Write a Python program that takes three numbers as input and checks if all three numbers are positive.
4.2 Write a Python program that takes a year as input and checks if it is a leap year (divisible by 4, but not divisible by 100 unless also divisible by 400).
4.3 Write a Python program that checks if a user is eligible for a discount based on their age (under 18 or over 65) and membership status (must be a member)

5.1 Greeting Creator: Write a program that asks the user for their first name and last name, then concatenates them to create a full name greeting.
5.2 Sentence Builder: Create a list of words and use the join() method to form a sentence. Experiment with different separators.
5.3 Address Formatter: Ask the user for their street address, city, state, and zip code. Concatenate these inputs to create a properly formatted address string.

6.1 Extracting Initials: Write a program that asks the user for their full name and extracts their initials using string slicing.
6.2 Reversing Words: Write a function that takes a sentence as input and reverses each word in the sentence using string slicing.
6.3 Palindrome Checker: Write a program that checks if a given string is a palindrome (reads the same forwards and backward) using string slicing.